services:
  backend:
    gpus: all
    environment:
      WHISPER_DEVICE: "cuda"
      WHISPER_COMPUTE_TYPE: "auto"
      NVIDIA_VISIBLE_DEVICES: "all"
      NVIDIA_DRIVER_CAPABILITIES: "compute,utility,video"
